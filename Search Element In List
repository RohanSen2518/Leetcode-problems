#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int N,ele, f=0;
    scanf("%d", &N);
    int arr[N];
    for (int i = 0; i < N; i++) {
        scanf("%d", &arr[i]);
    }
    scanf("%d", &ele);
  
    for (int i = 0; i < N; i++) {
        if (arr[i] == ele) {
            f=1;
            break;
        }
    }
    if(f==1){
        printf("Found\n");
    } else {
        printf("Not Found\n");
    }
    
    return 0;
}
